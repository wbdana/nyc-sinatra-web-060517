<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Check out this historical figure!</title>
  </head>
  <body>
    <h1><%= "#{@figure.name}" %></h1>
    <% @figure.titles.each do |title| %>
    <%= title.name %>
    <% end %>

  </body>
</html>
